fastapi
uvicorn[standard]
# No PyPI release; pinned to a commit for reproducibility (ADR-052).
madmom @ git+https://github.com/CPJKU/madmom@27f032e8947204902c675e5e341a3faf5dc86dae
aio-pika
python-dotenv
psycopg2-binary
minio
# Floor-pinned, not ==: YouTube regularly breaks older yt-dlp releases
# (signature/nsig, bot-check), and fixes only land in newer ones — a
# rebuild should be free to pick those up. Bump the floor when a stale
# lower bound would let an image resolve something already broken.
yt-dlp>=2026.8.19

# chord-cnn-lstm (ADR-052, apps/worker/vendor/chord_cnn_lstm) runtime deps.
# Install torch FIRST via the CPU-only index — the default PyPI wheel
# bundles CUDA and is much larger, and `pip install -r` alone can't be
# trusted to prefer the CPU build:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
# then this file as usual (torch already satisfied, no re-resolve).
torch
librosa
pretty_midi
h5py
joblib
pydub
